[jax_data]
# IP will be assigned by Tofu - update after provisioning
# jax-data ansible_host=192.168.1.102

# Placeholder using hostname (requires DNS or /etc/hosts)
jax-data ansible_host=jax-data.castle.shields.cc
